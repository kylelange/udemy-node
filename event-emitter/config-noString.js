/*writing the strings in this way takes away spelling errors
and makes bugs easer to find becasue they are here in the config*/

module.exports = {
  events: {
    GREET: 'greet',
    DONE: 'done',
    TEST: 'test'
  }
}
